<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bygen.mofeedb">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".KoinStarter"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mofeedb">
        <activity
            android:name="com.bygen.mofeedb.presentation.detail.DetailMovieActivity"
            android:label="@string/detail_show"
            android:parentActivityName=".presentation.HomeActivity"
            android:theme="@style/Theme.Mofeedb.NoActionBar" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.HomeActivity" />
        </activity>
        <activity android:name=".presentation.HomeActivity"
            android:theme="@style/Theme.Mofeedb.NoActionBar"/>
        <activity android:name=".presentation.SplashActivity"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>